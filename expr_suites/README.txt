with_unlabeled: training with unlabeled target data

complex_model: training with more complex code generator (similar to shared gen + specific gen)

with_clf: train using classifier loss instead of pairwise similarity loss

To run the experiments, go to the respective folder and run run.py